# 云函数权限文档（首版）

## 1. 目的

规范云函数权限边界，避免越权访问与数据泄露。后续每新增或修改云函数，必须先检查并更新本文件。

## 2. 角色定义

- `admin`：系统管理员，具备配置与审计权限
- `researcher`：算法研发，允许读取脱敏数据与写入推理结果
- `annotator`：标注人员，仅可访问标注任务相关集合
- `viewer`：只读查看，禁止导出敏感数据

## 3. 云函数权限矩阵（初版）

| 云函数名 | 允许角色 | 读权限 | 写权限 | 备注 |
|---|---|---|---|---|
| `submitInference` | researcher, admin | `imaging_studies` | `ai_inference_results` | 写入推理结果 |
| `createAnnotationTask` | researcher, admin | `ai_inference_results` | `annotation_tasks` | 生成主动学习任务 |
| `reviewAnnotationTask` | annotator, admin | `annotation_tasks` | `annotation_tasks` | 更新标注状态 |
| `queryStudyResult` | viewer, researcher, admin | `imaging_studies`,`ai_inference_results` | 无 | 只读查询 |
| `inferWithImage` | researcher, admin | `imaging_studies` | `ai_inference_results`,`inference_artifacts` | 上传影像并返回风险分数与Grad-CAM热图 |
| `infer` | researcher, admin | `imaging_studies` | `ai_inference_results` | 文本/结构化推理，受API Key与限流保护 |
| `routeInfer` | researcher, admin | `imaging_studies`,`posture_stub_config` | `ai_inference_results`,`security_access_logs` | 自动路由入口，返回路由理由与置信度；支持`route_mode`强制路由；坐姿分流当前为占位 |
| `postureAnalyze` | researcher, admin | `posture_stub_config` | `security_access_logs` | 坐姿占位分析接口（后续替换为关键点检测流程） |

## 4. 安全策略

- 所有函数入口执行JWT/会话鉴权与角色校验
- 默认拒绝策略：未显式授权即拒绝访问
- 写操作必须记录审计日志（操作者、时间、对象、变更摘要）
- 导出类能力仅`admin`可触发，且需二次确认
- 推理接口统一要求`X-API-Key`鉴权，缺失返回401、无效返回403
- 推理接口默认按“API Key + IP”维度限流，超限返回429
- 跨域访问仅允许白名单来源（本地联调域名与GitHub Pages域名）

## 5. 风险与后续动作

- 风险1：测试环境权限过宽  
  后续动作：测试/生产使用独立密钥与权限模板。

- 风险2：函数新增后未同步权限文档  
  后续动作：将“文档更新”加入PR检查清单。

## 6. 变更记录

- 2026-04-16：初始化云函数权限文档（v1.0）
- 2026-04-16：新增`inferWithImage`权限项，补充可解释产物写入权限（v1.1）
- 2026-04-16：明确`inferWithImage`产物为Grad-CAM热图（v1.2）
- 2026-04-16：新增`infer`权限项，补充API Key鉴权与限流策略（v1.3）
- 2026-04-16：补充跨域来源白名单策略，约束前端调用来源（v1.4）
- 2026-04-22：新增`routeInfer`与`postureAnalyze`权限项，支持自动路由与坐姿占位能力（v1.5）
- 2026-04-22：`routeInfer`新增`route_mode`参数，支持前端手动偏好路由（v1.6）
